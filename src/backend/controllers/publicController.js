'use strict';

/**
 * Public controller: рендер главной страницы и др. публичных страниц
 * Логическая декомпозиция: собирает данные для view (meta, hero, preview)
 */

exports.renderHome = (req, res) => {
  // Простейшие значения для MVP — в будущем можно подгружать из data/settings.json или из репозитория
  const meta = {
    title: 'ReferralMVP — реферальная платформа для вашего продукта',
    description: 'Запустите реферальную программу и продающие страницы для компаний за минуты. Простая интеграция и прозрачная статистика для партнёров.',
  };

  const hero = {
    heading: 'ReferralMVP — простая реферальная программа для вашего продукта',
    lead: 'Запустите реферальную страницу для каждой компании за минуты. Привлекайте клиентов через партнёров и отслеживайте рефералы в одном месте.',
  };

  res.render('index', Object.assign({}, meta, hero));
};
