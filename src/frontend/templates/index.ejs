<!doctype html>
<html lang="ru">
<head>
  <!-- HEAD partial (meta и стили) -->
  <%- include('partials/head') %>
</head>
<body>
  <!-- HEADER partial (сохранён, не меняем) -->
  <%- include('components/header') %>

  <!-- MAIN: продающая секция + регистрационная форма (partial) -->
  <main id="main-content">
    <section class="hero">
      <div class="container hero-inner">
        <div>
          <h1><%= heading || 'ReferralMVP — простая реферальная программа для вашего продукта' %></h1>
          <p class="lead"><%= lead || 'Запустите реферальную страницу для каждой компании за минуты. Привлекайте клиентов через партнёров и отслеживайте рефералы в одном месте.' %></p>

          <!-- Включаем форму регистрации (partial) -->
          <%- include('partials/registerForm') %>
        </div>

        <aside class="preview-card">
          <strong>Превью компании</strong>
          <p style="margin-top:8px;color:#566b75;font-size:14px;">Карточка превью продающей страницы компании (здесь будет реальный preview).</p>
        </aside>
      </div>
    </section>
  </main>
</body>
</html>
