<!doctype html>
<html lang="ru">
<head>
  <%- include('partials/head') %>
</head>
<body>
  <%- include('components/header') %>

  <main class="auth-page" role="main">
    <div class="auth-shell container">
      <div class="auth-card" role="form">
        <div class="auth-brand">
          <a href="/" class="logo" aria-label="ReferralMVP Home">
            <span class="logo-mark">
              <!-- small svg -->
              <svg width="36" height="36" viewBox="0 0 28 28" fill="none" xmlns="http://www.w3.org/2000/svg" role="img">
                <defs><linearGradient id="lg2" x1="0" x2="1" y1="0" y2="1"><stop offset="0%" stop-color="#0052CC"/><stop offset="100%" stop-color="#2684FF"/></linearGradient></defs>
                <rect rx="6" width="28" height="28" fill="url(#lg2)"></rect>
                <text x="50%" y="54%" text-anchor="middle" fill="#fff" font-family="Segoe UI, Roboto, Arial" font-size="14" font-weight="700">R</text>
              </svg>
            </span>
            <span class="logo-text">ReferralMVP</span>
          </a>
        </div>

        <div class="auth-body">
          <h1>Создать аккаунт</h1>
          <p class="auth-desc">Укажите электронную почту или телефон и создайте пароль.</p>

          <form id="register-form" novalidate>
            <div class="form-block">
              <label for="email">Электронная почта</label>
              <input id="email" name="email" class="text" type="email" value="<%= typeof email !== 'undefined' ? email : '' %>" <%= typeof email !== 'undefined' ? 'readonly' : '' %> />
            </div>

            <div class="form-block">
              <label for="phone">Телефон (альтернативно)</label>
              <input id="phone" name="phone" class="text" type="tel" value="<%= typeof phone !== 'undefined' ? phone : '' %>" <%= typeof phone !== 'undefined' ? 'readonly' : '' %> />
            </div>

            <div class="form-block">
              <label for="password">Пароль</label>
              <input id="password" name="password" class="text" type="password" placeholder="Минимум 8 символов" />
            </div>

            <div class="form-block">
              <label for="confirm">Подтвердите пароль</label>
              <input id="confirm" name="confirm" class="text" type="password" />
            </div>

            <div class="form-actions">
              <button id="btn-register" type="button" class="aui-button aui-button-primary">Зарегистрироваться</button>
              <a href="/" class="aui-button aui-button-link">Отмена</a>
            </div>

            <div id="reg-help" class="form-help" aria-live="polite"></div>
          </form>
        </div>
      </div>
    </div>
  </main>

  <script src="/public/js/register.js" defer></script>
</body>
</html>
